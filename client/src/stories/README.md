# Component stories

Minimal Storybook 10 (`@storybook/react-vite`) for Brand / Screen. Vite 8 compatible.

```bash
npm install --prefix client
npm run storybook --prefix client
```

OrderTicket stories are deferred until that component is extracted.
