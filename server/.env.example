# Copy to .env locally, or set on Render.
PORT=4000
CLIENT_ORIGIN=http://127.0.0.1:5173,https://arth-broker.vercel.app
JWT_SECRET=change-me
REFRESH_TOKEN_PEPPER=change-me-too

# Arth Coach — OpenAI gpt-4o-mini (optional; local fallback works without it)
OPENAI_API_KEY=
